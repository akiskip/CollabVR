using UnityEngine;
using System.Collections;

public class CameraLights : MonoBehaviour
{

	//1. get the user camera object
	// 2. deactivate light for the user
		// 3. when a new user connects, Get the Light of other cameras
			// 4. activate light of other users
		// Use this for initialization

	/*


	void Start () {


			// Make a game object
		lightGameObject : GameObject = new GameObject("The LaserSight");
			
		// Add the light component
		lightComp = lightGameObject.AddComponent.<Light>();
			
			// Set color and position
		lightComp.color = Color.red;
			
		// Set the position (or any transform property)
		lightGameObject.transform.position = Vector3(0, 1.5, 0.25);
		}
	Light myLight = lightPoint.GetComponent("Light");
	
	if (Input.GetButtonDown("Fire1")) {
		myLight.enabled = !myLight.enabled;
	}

*/

}

