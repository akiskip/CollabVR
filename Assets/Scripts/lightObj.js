#pragma strict


var lightComp : GameObject;
lightComp = GameObject.Find("lightGameObject2");

function Start () {

/*
// Make a game object
	var lightGameObject : GameObject = new GameObject("The Light");

	// Add the light component
	var lightComp = lightGameObject.AddComponent.<Light>();

	// Light Type
	
	lightComp.type = LightType.Spot;
	lightComp.spotAngle = Random.Range(10,120);
*/	
	/*
	
	// Set color and position, intensity
	
	lightComp.color = Color.blue;
	lightComp.intensity = 5.81;
	
	*/
/*	
	// Set the position (or any transform property)
	lightComp.transform.position = Vector3(0, 1.5, 0.25);

// turning the light on

	lightComp.SetActive(true);

}

function Update () {

// key trigger

  if(Input.GetKeyDown(KeyCode.E)){
  LightComp.active = false;

}
*/
}